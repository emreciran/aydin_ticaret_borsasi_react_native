import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    padding: 6,
    gap: 2
  },
  item: {
    flexDirection: "row",
  },
  text: {
    fontSize: 15,
  },
});

export default styles;
